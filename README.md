# Simple React Website

A minimal React website using Vite.

## Prerequisites

- Node.js 18+

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL shown in terminal (usually `http://localhost:5173`).

## Production build

```bash
npm run build
npm run preview
```